#include<bits/stdc++.h>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        else if(binary_search(s.begin(),s.end(),'a'))
        cout<<-1<<endl;
    }
    return 0;
}